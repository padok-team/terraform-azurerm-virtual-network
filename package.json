{
    "description": "npm version",
    "scripts": {
        "version": "standard-version --no-verify",
        "dry-run": "standard-version --dry-run --no-verify"
    },
    "version": "1.0.0",
    "dependencies": {
        "standard-version": "^9.3.2"
    }
}
